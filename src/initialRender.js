import { inventoryRender } from "./inventory";import { productSelect, productTable } from "./selectors";import { productInfo } from "./states";
const initialRender = () => {
  
  inventoryRender();
  
};

export default initialRender;
